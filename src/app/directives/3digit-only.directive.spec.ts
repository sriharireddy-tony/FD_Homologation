import { 3digitOnlyDirective } from './3digit-only.directive';

describe('3digitOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new 3digitOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
