import { TwodigitsOnlyDirective } from './twodigits-only.directive';

describe('TwodigitsOnlyDirective', () => {
  it('should create an instance', () => {
    // const directive = new TwodigitsOnlyDirective();
    // expect(directive).toBeTruthy();
  });
});
